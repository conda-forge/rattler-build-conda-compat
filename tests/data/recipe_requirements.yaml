requirements:
  build:
    - ${{ compiler('c') }}
    - if: linux
      then:
        - ${{ cdt('xorg-x11-proto-devel') }}
  host:
    - python
  run_exports:
    weak:
      - if: osx
        then:
          - weak-then
        else:
          - weak-else
      - weakreq
    strong:
      - strongreq
