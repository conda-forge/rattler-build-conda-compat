package:
    name: py-test
    version: 1.0.0
requirements:
    build:
      - "${{ compiler('c') }}"
      - if: win
        then:
          - ruby
    host:
        - python
    run:
        - python
